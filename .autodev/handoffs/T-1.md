# Handoff — T-1, the digest engine

**Written:** 2026-08-26, end of the first build session. (Supersedes the 2026-08-25 handoff.)
**State:** COMPLETE through `verify`. All 9 acceptance criteria met. On `main` at
github.com/BMA-Corgea/repo-tour. Parked at **uat**, waiting on Evan's `accept` gate —
that gate's policy is `human` and only he can clear it.
**Next actor:** whoever picks up T-1. Read `.autodev/specs/T-1-digest-engine.md` first,
then `.autodev/reports/T-1-plan.md` for the per-criterion status.

## Where things stand

Four commits on `T-1-digest-engine`, nothing pushed (no git remote is configured):

| Commit | What |
| --- | --- |
| `948de1e` | stages 1–3: inventory, extract, rank |
| `9cb90c9` | KB lessons, code map, locate + plan reports |
| `a03d92a` | auto-review fix: chunked hashing instead of whole-file reads |
| `6655789` | criterion 8: the self-contained HTML view |

19 tests pass (`npx vitest run`). Typecheck and build are clean. `npm audit --omit=dev`
reports 0 production vulnerabilities; the 5 dev-tree findings (vite → esbuild, under
vitest) are waived and never shipped.

## Acceptance criteria — honest status

| # | Criterion | State |
| --- | --- | --- |
| 1 | nested repos found | **met** — tested; proven on an 8-repo tree |
| 2 | classification honest | **met** — tested; every classification carries its signal |
| 3 | ranking beats length | **met** — tested; `manifest.yaml` ranks #6 of 11,959 on GUTS |
| 4 | extraction exact | **met** — hand-written expectations, exact equality |
| 5 | rollup holds | **met** — 1,846 tiers on GUTS in 62ms; every tier hashed from children |
| 6 | incremental works | **met** — proven live: 100% reuse on a no-change run, 97% with 1 edit + 1 rename |
| 7 | cost reported | **met** — reported even when zero |
| 8 | inspectable HTML view | **met** — `src/view.ts`; self-containment enforced by test |
| 9 | runs on an unseen repo | **met** — ran cold on GUTS, no config, no prior state |

**All nine met.** The merge gate was cleared by `policy:unattended` (the shop's own rule for
this preset, with every check green). The `accept` gate was NOT cleared and must not be —
its policy is `human` and its keyholder is Evan.

## What to build next, in order

1. **Stage 4 — interpret.** The only paid stage, and now the only unbuilt one. It gets the
   stage-3 skeleton and is asked what things MEAN, never what is in them. Two price points:
   a cheap sweep over everything not floored to zero, and a deep pass over the ranked slice
   (965 files on GUTS by the default cut). Report tokens per tier — criterion 7 already
   has the fields.
2. **The drift-budget spike.** Before any constant from spec §5 goes near the code. They are
   still not written down anywhere executable, and that is correct.
3. **T-2, the real tour player.** What exists today is a tour over the *digest view*. The
   product Evan described is a tour over a GitHub-repo-lookalike UI with a notes panel that
   tags each note with the step that provoked it. `src/tour.ts` proves steps can be generated
   from digest data; the player and the notes panel are the actual T-2/T-3 work.

## Traps — the 2026-08-25 list, CORRECTED by measurement

Two entries in the previous handoff were wrong, and both were inherited from the
throwaway Python probe. Full detail in `kb/wiki/lessons.md`.

- ~~"GUTS: 6 nested repos / 2,505 files"~~ → **8 repos / 11,959 files.** The probe missed a
  repo nested two levels deep entirely. Re-derive any number that came from it.
- ~~"The import graph underdelivers: 124 edges across ~1M lines"~~ → **3,247 edges, 42%
  coverage** (4,439 of 10,688 imports resolved). The graph is far healthier than feared.
  **Keep the honesty requirement anyway** — 6,249 imports still leave the tree, and
  cross-service topology is genuinely absent.

Still true and still worth fearing:

- **Length ranks a repo backwards** unless LOC is weighted third AND log1p-normalized.
  Both mechanisms are required; either alone fails.
- **The drift-budget constants are invented.** They are not in the code at all yet. Keep
  it that way until a spike measures them.
- **Stage 2 must RUN after stage 3** even though it is numbered before it — in-degree is a
  product of the import graph. Reordering these produces plausible output and no error.

## The decision Evan has made, and the one still open

- **MADE 2026-08-26: Node + TypeScript** with `web-tree-sitter`. Recorded in
  `kb/wiki/decision-implementation-language.md`. Not to be relitigated.
- **STILL OPEN: T-1's width.** The spec keeps T-1 narrow — digest plus a browsable view,
  no tour. Evan approved that shape but has still not seen a tour. §9 of the spec flags
  widening T-1 as a live option. If he asks "where's the tour", the answer is T-2, by his
  own approval.

## How Evan wants to be written to

Explain AutoDev's process vocabulary inline on first use — gate, stage, spike, ticket,
passport are the tool's words, not his. He is the keyholder on every human gate here, so
a decision written in vocabulary he does not hold is one he cannot make.
