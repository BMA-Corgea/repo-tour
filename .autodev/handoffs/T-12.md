# Handoff — T-12, the build-order engine

**Written:** 2026-09-04, close-out.
**State:** DONE. All 9 acceptance criteria met and checked by a real test each.
`npm run typecheck && npm test` green (154 tests, 2 files). Code map regenerated.
**Branch:** `feature/T-12-build-engine`, worktree `../repo-tour-T-12`. Not merged, not
pushed, no PR opened — the orchestrator merges to `main` after T-11 per the plan's
delivery target.

## Commits, in order

| Commit | What |
| --- | --- |
| `04997db` | `src/build/types.ts` (the T-1 spec §4.2 data model verbatim) + `src/build/witness.ts` |
| `943f382` | write-ahead handoff before the chapter/fallback logic (plan step 3a) |
| `ceb3540` | `src/build/plan.ts` — `buildPlan()`: chapters, file order, steps — plus most of `test/build.test.ts` (AC1-4, AC7 partial) |
| `00fdb04` | `src/build/stub.ts` — the scaffold writer, plus AC5 tests |
| `c3f6c5f` | `src/build/check.ts` — the structural check, plus AC6 tests |
| `efdd8fa` | `schema/build-plan.schema.json` + hand-rolled validator, plus AC8 tests |
| `fee7f66` | `src/build/index.ts` + `plan <path> [--json]` wired into `src/cli.ts`, plus AC9 tests |
| `4bca837` | closed two coverage gaps found on review: AC7's docstring-`why` branch, AC4's zero-commit (as opposed to no-`.git`-at-all) branch |
| `a3a8a13` | regenerated `kb/CODE-MAP.md` |

**Touched-files:**
`src/build/types.ts`, `src/build/witness.ts`, `src/build/plan.ts`, `src/build/stub.ts`,
`src/build/check.ts`, `src/build/index.ts`, `schema/build-plan.schema.json`,
`src/cli.ts` (edited — added the `plan` verb beside `digest`/`tour`/`inspect`),
`test/build.test.ts` (new), `kb/CODE-MAP.md` (regenerated), `.autodev/handoffs/T-12.md`
(this file), `kb/CURRENT-WORK.md` (one line under Live edge).
Nothing on the DO-NOT-TOUCH list was touched: `package.json`, `src/extract.ts`,
`src/skins.ts`, `src/interpret.ts`, `src/ask.ts`, `src/server.ts`, `src/repoview.ts`,
`.autodev/tickets/`, `.autodev/events.jsonl`, `.autodev/releases/` are all untouched by
this branch's commits. (`package-lock.json` and `.autodev/conformance-history.jsonl`
show as locally modified in the worktree from `npm install` and a background monitor
respectively — neither was staged or committed.)

## Where things are, and what each does

- `src/build/types.ts` — `BuildPlan`, `Chapter`, `Step`, `Option`, `Range`, `CheckReport`,
  verbatim from the VSCode-LLM-Tutorial spec plus the plan's own `CheckReport`/
  `reproduce` additions. Full shape from day one; v1 only ever produces `mode: 'recreate'`.
- `src/build/witness.ts` — `firstCommits(repos)`. One `git log --diff-filter=A --name-only`
  walk per repo root, oldest-first.
- `src/build/plan.ts` — `buildPlan(digest, { root, witness? })`. The whole engine: chapters
  from `buildArchitecture` (bottom-up, foundations first), a fallback to raw
  `digest.tiers` when architecture yields fewer than two parts, file ownership by
  longest-prefix with a synthetic `@misc` catch-all, per-chapter topological file order
  with tests spliced after their subject, up to 5 symbol steps per file by span
  (overlap-aware, so an exported class does not double-claim its own methods' ranges),
  and a two-pass `dependsOn` resolution once every step id exists.
- `src/build/stub.ts` — `stubFile(source, loadBearing, language, questions)`. TS/JS/TSX
  and Python, with a "leave it alone" fallback for anything neither pattern fits.
- `src/build/check.ts` — `check(learnerSource, referenceExtract, language, relPath)`.
  Structural only: symbols by (name, kind), imports by raw specifier, parse errors.
- `src/build/index.ts` — the one public re-export surface (`./build` in package.json,
  once T-11 lands it, resolves to `dist/build/index.js`).
- `schema/build-plan.schema.json` — draft-07, the whole `BuildPlan` shape.
- `src/cli.ts` — `plan <path> [--json]` added beside `digest`/`tour`/`inspect`, reusing
  the same arg parser and the same `digest()` call.
- `test/build.test.ts` — 38 tests, one `describe` block per AC (some ACs get two: a
  primary-path fixture and a dedicated edge-case fixture). Every fixture with history is
  a real git repo built with `execFileSync('git', …)`, `test/pipeline.test.ts`'s own style.

## The risky part, and how it actually went

The write-ahead handoff (commit `943f382`) called out plan step 3a as the risk: a small
fixture can make `buildArchitecture` yield fewer than two parts, and the tier-fallback
path needed its own test rather than an assumption that it degrades gracefully from the
primary path. It did need its own fixture (two single-file directories, each individually
too small for `chooseSubsystems`'s `directCode >= 2` filter), and that fixture caught a
REAL bug before it shipped (see Surprises) — the write-ahead was worth doing.

## Surprises, in the order they were found

1. **`git log --diff-filter=A --name-only --format=...` puts the blank line right AFTER
   each commit's header, not between commits.** A parser that resets "current commit" on
   every blank line drops every filename, because the reset fires immediately before the
   names it should capture. `witness.ts` detects a header by shape (tab-delimited
   `sha\tdate\tsubject`) instead. Verified by hand with a throwaway repo before writing
   the real parser — worth a `kb/wiki/lessons.md` entry (not added here: this ticket's
   touched set is `src/build/**` + `src/cli.ts` + tests + this handoff; a wiki edit is a
   small, separate, easy follow-up for whoever picks up T-13 or a later repo-tour ticket).
2. **A raw NUL byte reached the file on disk from a template-literal separator that was
   only ever meant to be a plain, printable character**, in `chapterEdges`'s dedup key.
   Whatever layer carried the file content from
   this session's own tool call into the file interpreted the escape rather than passing
   it through literally, so a real `0x00` byte landed in `src/build/plan.ts` — which then
   made plain `grep` treat the entire file as binary and silently print nothing, no error,
   for every query. Found because a routine `grep -c "function" plan.ts` returned nothing
   at all, which should never happen. Fixed by replacing the byte with a plain space
   (`python3 -c "...replace(b'\x00', b' ')..."`, operating on raw bytes so the fix itself
   could not repeat the mistake) and re-verified with `file`/`grep`. **Lesson for anyone
   editing files as this kind of agent: never put a `\uXXXX` escape literally in file
   content passed through a tool call; if a template literal genuinely needs one, write
   it in a way that cannot be pre-decoded, or add it with a follow-up edit and check the
   resulting bytes.**
3. **`extract.ts` marks a class's public methods `exported: true` whenever the class
   itself is exported**, so a class and its own methods can both be candidates for the
   5-symbol cap, with the class's range fully containing its methods' ranges. Resolved by
   sorting candidates by span descending and skipping any that overlap one already
   chosen — not spec text, a considered resolution of a case the spec did not anticipate.
4. **A tree with NO `.git` anywhere makes `rollup.ts#kindFor` mislabel the scan root
   itself `kind: 'subsystem'`** (no `RepoRef` means `isRepoRoot` is never true, even for
   the root), which would have given the tier-fallback path a phantom, file-less chapter.
   `tierSeeds()` now excludes `path === ''`, mirroring `chooseSubsystems`'s own existing
   guard, plus a general safety net in `buildPlan` itself that drops any seed that ends
   up owning zero files directly (belt and suspenders — the second guard would have
   caught this on its own even without the first).
5. Nearly repeated `test/pipeline.test.ts`'s own documented "paths with spaces in them"
   defect while writing the AC8 schema test: `new URL(import.meta.url).pathname`
   percent-encodes the space in this project's own directory name
   (`.../Coding Projects/...`). Used `fileURLToPath` instead, per that file's own lesson.
6. `code-map.mjs`'s `SKIP_DIRS` treats any directory literally named `build` as a
   build-output directory (same bucket as a root-level `dist/` or `build/`), so
   `src/build/**` — this ticket's own module — is invisible to the regenerated
   `kb/CODE-MAP.md`. Not fixed (the script is AutoDev plugin infrastructure, out of this
   ticket's touched set); flagged here for whoever next touches that script, or names a
   source directory `build` in any repo it runs against.

## Definition of done — verified per criterion

| AC | Check | Result |
| --- | --- | --- |
| 1 | fixture DAG, exact equality | met — two fixtures: primary (architecture) path and the tier-fallback path |
| 2 | kinds/caps/reproduce | met — kinds, 5-symbol cap by span, test placement, reproduce list, all asserted |
| 3 | ids stable across body edit | met — also asserted a rename DOES change the id |
| 4 | witness from history, null without | met — no-`.git`, zero-commit `.git`, and real-history cases all covered |
| 5 | stub parses TS/JS/Python, 0 ERROR nodes | met — 7 stub shapes tested, each re-run through the real `extract()` |
| 6 | `check()` — renamed local passes, missing export named | met — plus extra-export, missing-import, and unparseable-learner cases |
| 7 | `options.length >= 1`, docstring `why` | met — single-author-option invariant plus the docstring/no-docstring branches |
| 8 | schema validates a generated plan, rejects bad `mode` | met — plus a missing-required-key case |
| 9 | CLI `plan <path> [--json]` | met — spawns the real `tsx src/cli.ts`, not an in-process call |

## Next actor

T-13 (interpret: alternatives per step; Ask context for a build step) is the next repo-tour
ticket in this ticket set's order (`T-11 ∥ T-12 ∥ T-2 → T-13 → T-3 → …`). It reads
`decision.options`/`authorChoice`/`why`/`whySource` from this module and raises
`options.length` to `>= 2` by calling the model for alternatives — it does not need to
change anything in `src/build/plan.ts`'s ordering or step-shape logic. Also worth a look
before T-13 starts: the two follow-ups noted above under Surprises 1 and 6 (a
`kb/wiki/lessons.md` entry for the git-log parsing trap; a possible fix or note in
`code-map.mjs` for the `build`-named-directory blind spot) — neither blocks T-13, both are
small and easy to pick up separately.
