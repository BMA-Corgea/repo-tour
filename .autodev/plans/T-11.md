# Plan — T-11 (branch `feature/T-11-core-package`, worktree `../repo-tour-T-11`)

Delivery target: local ticket branch with commits; the orchestrator merges to `main` and pushes.

1. **`src/assets.ts`.** The three-tier resolution (explicit config → env → default), with a `resetAssets()` for tests. Default assets dir = `<pkg>/assets`; default grammars dir = `dirname(require.resolve('tree-sitter-wasms/package.json'))/out`. Verify: unit test of each tier.
2. **Rewire** `src/skins.ts#skinsDir` and `src/extract.ts#grammarPath` onto it. Verify: existing suite green; `./repo-tour doctor` still finds grammars with nothing set.
3. **`package.json`.** `exports`: `./digest ./interpret ./llm ./ask ./skins ./extract ./types ./rollup ./architecture ./codetour ./assets ./build ./package.json` (each `{ types: ./dist/x.d.ts, default: ./dist/x.js }`; `./build` → `./dist/build/index.js` — the file does not exist until T-12 lands; a missing export target is not an error at install time, and the consumer test skips it if absent), plus `"./dist/*": "./dist/*"`. `files: ["dist", "assets", "schema", "README.md", "LICENSE.txt"]`. `scripts.prepare: "npm run build"`. Verify: `node -e "import('repo-tour/skins')"` from a temp dir with `node_modules/repo-tour` symlinked to the repo resolves; so does `repo-tour/dist/skins.js`.
4. **`test/consumer.test.ts`.** Creates a temp dir with the symlink; spawns `node --input-type=module` importing every export, asserting each resolves (skip `./build` if `dist/build/index.js` is absent, and say so); runs `digest()` on a tiny fixture through the package; `baseCss()` non-empty; `alternateCss()` includes `gunmetal`; override test: copy `assets/` to a temp dir, `configureAssets({assetsDir})`, `baseCss()` equal. `npm pack` into a temp dir and `npm install <tarball>` → `dist/` present (AC3). Verify: `npx vitest run test/consumer.test.ts` green.
5. **README** one line: "Consumed as a package by the VS Code front end: `file:../repo-tour` works because `dist/` is committed-built; a git dependency triggers `prepare`."
6. **Close-out.** `npm run typecheck && npm test`; `node <plugin>/scripts/code-map.mjs --root .`; handoff `.autodev/handoffs/T-11.md` with `Touched-files:`; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead: **step 3 — TypeScript `moduleResolution: bundler` + an `exports` map with `types` conditions.** If the consumer's typecheck cannot see the `.d.ts` through the map, add `typesVersions` as the fallback and note it.

Negative bands: no new runtime dependency; no behaviour change to the CLI or server; never touch `src/build/**`.
