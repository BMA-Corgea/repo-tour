# Plan — T-13 (branch `feature/T-13-interpret-decisions`, worktree `../repo-tour-T-13`, cut from `main` AFTER T-12 merges)

Delivery target: local ticket branch with commits; the orchestrator merges to `main` and pushes.

1. **`src/interpret.ts` — the runner seam first.** `InterpretOptions.runner?: typeof runLlm`; `interpretStops` (and `interpretArchitecture`, same one-line change) call `opts.runner ?? runLlm`. Verify: existing suite green; a new test injects a runner returning canned JSON and asserts `interpretStops` used it (no real `claude` spawned — assert by the canned text landing in `meanings`).
2. **The prompt and the shape.** `StopMeaning.alternatives: Array<{ label: string; consequence: string }>`; `PROMPT_VERSION = 6`; `SYSTEM` adds: `- "alternatives": exactly two OTHER reasonable ways this could have been done, each as {"label": "...", "consequence": "..."} — what it would have cost or changed. Real alternatives an experienced engineer would weigh, not strawmen.` and the JSON line becomes `[{"n":1,"what":"...","why":"...","summary":"...","alternatives":[{"label":"...","consequence":"..."},{"label":"...","consequence":"..."}]}]`. `parseAnswer` maps `alternatives` (array of objects with string `label` + `consequence`, else `[]`). The cached-entry read path fills a missing `alternatives` with `[]` (a v6 entry always has the field; the guard is cheap honesty). Verify: prompt test asserts the field and version; parse test with and without the field.
3. **`src/build/interpret.ts`.** `interpretPlan(plan, digest, opts: InterpretOptions & { root: string })`:
   a. steps → `CodeStep[]`: `symbol` steps use `target.startLine/endLine`; `file` steps use `1 … min(loc, 120)` (loc from `digest.inventory.files`); `shape` steps skipped; `title` = the step's question.
   b. `interpretStops(root, codeSteps, digest.inventory.files, digest.extracts, digest.graph.edges, opts)`.
   c. fold: for each step with a meaning at `stepKey(file, start, end)`: `options = [{ ...author, consequence: meaning.summary || author.consequence }, ...alternatives.map((a, i) => ({ id: `alt-${i + 1}`, label: a.label, consequence: a.consequence, taken: false }))]`, `why = meaning.why || why`, `whySource = 'interpret'`; untouched steps keep T-12's values. Returns a NEW `BuildPlan` with `cost = result.cost` and a fresh `generatedAt`. Never mutates the input.
   Verify: fixture + stubbed runner → `options.length === 3` on interpreted steps, `whySource === 'interpret'`, the input plan deep-equal to before, `cost.metered === true`, `cost.calls` = number of files with steps.
4. **`src/build/index.ts`** re-exports `interpretPlan`. Verify: typecheck; `import('repo-tour/build')` from a temp dir lists it.
5. **`src/ask.ts`.** `AskContext.build?` + rendering under `--- THE BUILD STEP ---` (question, each option as `[author]`/`[alternative]` with consequence, why, then `The learner's file against the author's:` + clipped diff when present). Verify: unit test asserts the block text; `ASK_PERSONA` unchanged (assert by equality with a snapshot string in the test).
6. **`src/cli.ts`.** `plan <path> [--json] [--interpret [--cached-only]]` — with `--interpret`, after `buildPlan` run `interpretPlan` with `{ provider: args.provider, model: args.model, cachedOnly: args.cachedOnly, onProgress: quiet ? undefined : console.error }` and write the interpreted plan; print the cost line (`metered: false` printed as "this provider does not report usage"). Verify: `npx tsx src/cli.ts plan <fixture> --interpret --cached-only` runs with no model available and writes a plan (zero interpreted, honest cost).
7. **Cache reuse (AC5).** Test: run `interpretPlan` twice with the stubbed runner counting calls — the second run makes zero runner calls (`cachedStops === interpretedStops` of the first). Uses `opts.cacheDir` under the fixture's temp dir.
8. **Close-out.** `npm run typecheck && npm test`; code map regen (`node <plugin>/scripts/code-map.mjs --root .` — note it skips `src/build/` by a pre-existing `SKIP_DIRS` quirk; say so in the handoff, do not "fix" the tool); handoff `.autodev/handoffs/T-13.md` with `Touched-files:`; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead: **step 2 — bumping `PROMPT_VERSION` invalidates every cached repo-tour explanation on this machine** (`.cache/interpret/`); that is by design (spec: "old cached answers were produced by a different question") but say it in the handoff so nobody reads a full re-interpretation as a regression.

Negative bands: no edit to `src/build/plan.ts` / `check.ts` / `stub.ts` / `witness.ts`, `package.json`, `extract.ts`, `skins.ts`, `server.ts`; no change to `ASK_PERSONA`; never touch the ledger.
