# Plan - T-15 (branch feature/T-15-script-symbols, worktree ../repo-tour-T-15)

Delivery target: local ticket branch with commits; the orchestrator merges to main and pushes.

1. Repro tests first, committed RED: test/build-scripts.test.ts with (a) an IIFE fixture (sql-gauntlet's shape: '(function () { function init(){...} function render(){...} document.addEventListener(...) })();') asserting extract records init/render with real ranges; (b) a CommonJS server fixture ('const http = require("http"); function handle(req,res){...} function listRepos(){...} http.createServer(handle).listen(0)') asserting buildPlan yields symbol steps for handle/listRepos and none for http; (c) a negative: a helper nested inside a normal top-level function is NOT recorded; (d) an arrow IIFE '(() => { ... })()' and the '!function(){}()' form. Verify: the four fail on main's code.
2. extract.ts: in the top-level visitor, detect the IIFE shapes (expression_statement -> call_expression whose function is a parenthesized_expression wrapping function/arrow_function, or a unary_expression '!' over such a call) and recurse ONE level into the function body's statement_block, recording declarations exactly as top-level ones (exported:false, doc from the leading comment as today). Verify: (a), (c), (d) green.
3. plan.ts: candidates = symbols.filter(exported); if empty, candidates = symbols. Then filter: kind in {function, method, class} OR (kind === 'variable' && span >= 3). Then the existing span sort, overlap-skip, cap 5. Verify: (b) green; the existing T-12 fixtures produce identical plans (snapshot the ids of one existing fixture before and after - they must not change).
4. Live check: 'npx tsx src/cli.ts plan /home/corgea/Desktop/Coding Projects/sql-gauntlet' - every named-function .js file has >= 1 symbol step; record the counts in the handoff (this READS sql-gauntlet and writes only its .repo-tour/ cache, which repo-tour already owns there).
5. Close-out: npm run typecheck && npm test (167 existing + new); code map regen; handoff .autodev/handoffs/T-15.md with Touched-files:; one line in kb/CURRENT-WORK.md.

Risk to write-ahead: step 2 - tree-sitter's node type names for the IIFE shapes differ between the javascript and typescript grammars (parenthesized_expression, call_expression, arrow_function, function_expression vs function). Confirm each with a tiny parse dump before writing the matcher; do not guess.

Negative bands: no change to interpret/ask/cli/build-interpret/index (T-13); no change to Python or ESM/TS symbol behaviour; never touch the ledger.
