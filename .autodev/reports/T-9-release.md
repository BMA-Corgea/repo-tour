# T-9 — release

`main` @ `872a112` (PR #5). No production service; "released" means it builds and runs from a
clean `main` — `tsc` clean, 110 tests, and the click-path driven against real PR #3.
Rollback: `git revert`.
