Verdict: REWORK

# T-15 review — script-style JS yields no load-bearing ranges

Reviewer seat, auto-review stage. Fresh session, adversarial pass, did not write this code.
Worktree: `/home/corgea/Desktop/Coding Projects/repo-tour-T-15`, branch
`feature/T-15-script-symbols`, diffed against `main` (`94e66e5`, the true base — T-15 and
T-13 both branch from it directly, no drift to account for). All commands below were run
live in this worktree; nothing was assumed from the handoff without independent
re-verification, and every experiment was reverted (`git status --short` is clean of
anything but the pre-existing, unrelated `.autodev/conformance-history.jsonl` background-
monitor edit noted in the handoff and left untouched, same as T-11/T-12 before it).

**Summary:** the ticket's own stated bug is genuinely fixed and unusually well verified —
bug-lane discipline is real (independently reproduced RED), the IIFE matcher's grammar
research checks out under independent parse-tree dumps, the live sql-gauntlet numbers match
the handoff exactly down to per-file symbol names, and the full suite is green at the
claimed 185/185. But adversarial testing of `src/build/plan.ts`'s new `isEligibleCandidate`
found it is wired to run against the **already-exported** candidate path too, not only the
new fallback path — which silently drops symbol steps from ordinary, already-working
TS/ESM files (any file whose real exports are `interface`/`type`/`enum`, or a short
exported constant), and separately makes previously-invisible **private Python** symbols
and **zero-export TS scripts** newly visible. This is a real, reproducible, untested,
undisclosed change to TS/ESM and Python behavior, and the spec's own "Out of scope" section
says that behavior is "unchanged by design." That is the one gating finding below; the rest
of the diff holds up.

---

## Angle 1 — bug-lane discipline: was `dd267cb` really RED on `main`?

**Checked:** confirmed `dd267cb`'s parent is `94e66e5` (`main`) exactly, then
`git checkout dd267cb -- src/extract.ts src/build/plan.ts test/build-scripts.test.ts` (pins
`src/`/`build/plan.ts` to base, keeps the new test file) and ran
`npx vitest run test/build-scripts.test.ts`.

**Result: real RED, restored cleanly.** 7 failed / 2 passed — exactly what `dd267cb`'s own
commit message claims ("(a), (b), and all five of (d)'s sub-cases" fail; "(c) and the
snapshot already pass today"). Restored with `git checkout HEAD -- src/extract.ts
src/build/plan.ts test/build-scripts.test.ts`, confirmed `git diff HEAD` empty, reran: 9/9
green. No finding.

---

## Angle 2 — `extract.ts`: `unwrapParens`/`iifeBody`, exactly how far it reaches

**Checked:** read the matcher (`src/extract.ts:227` `unwrapParens`, `:249` `iifeBody`, wired
at `:333`), then built a probe (`extractOf` against ten hand-written snippets, deleted after
use) exercising shapes never in the builder's own test file, plus an independent tree-sitter
parse-tree dump (`Parser`/`Language.load` directly, both grammars) to cross-check the
write-ahead handoff's claimed node shapes rather than trust them.

**Recursion depth — confirmed exactly one level, both directions:**
- A plain nested helper inside an ordinary top-level function: absent (builder's own test
  (c), reconfirmed).
- An IIFE nested *inside* a top-level IIFE (`(function(){ (function(){ function
  deepInner(){} })(); function shallowInner(){} })();`): `shallowInner` recorded,
  `deepInner` is not — `iifeBody` is only ever called from the top-level `for` loop
  (`extract.ts:333`), never again from inside the `if (body)` branch, so a nested IIFE's
  body is handed to plain `recordDecl` (`extract.ts:100`), which does not recognize
  `expression_statement` and drops it silently. Matches AC1's "recurse one level only"
  precisely — verified by running it, not by reading the code and assuming.

**Shapes tested beyond the four documented ones, and what each does:**
| Shape | Recognized? | Notes |
|---|---|---|
| `foo((function(){...})())` — IIFE as an argument | No | correct: the top-level statement's own call is `foo(...)`, not the anonymous function; matches AC1's literal "expression statement calling a function/arrow" (the outer call is what's inspected) |
| `const x = (function(){...})()` — module/revealing-module pattern | No (only `x` itself, kind `variable`) | correct per AC1's literal text ("a **top-level expression statement** calling…" — a `lexical_declaration` is not an expression statement), and the code's own docstring says so explicitly. **Not called out anywhere in the handoff's "Surprises," despite being a very common real-world script shape** (arguably as common as sql-gauntlet's bare form) — advisory, not gating, since the implementation is honest and exactly spec-width, not over- or under-claimed. |
| `(async () => { function inner(){} })()` | Yes | works for free (async doesn't change the `arrow_function` node type); not in the builder's test file |
| `(async function(){ function inner(){} })()` | Yes | same reason; not in the builder's test file |
| `(function(){...}).call(this)` | No | `unwrapParens` only unwraps `parenthesized_expression`; the callee is a `member_expression`, never reached — confirmed via a direct parse dump: `function: (member_expression object: (parenthesized_expression (function_expression...)) property: (property_identifier))`. Outside AC1's literal scope (not listed), so advisory. |
| `(function(){...}).bind(this)()` | No | same cause as `.call` |
| `!!function(){...}()` (double unary) | No | the code unwraps **one** unary level only (`extract.ts:255-259` does not loop); parse dump confirms two nested `unary_expression`s. Matches the code's own docstring ("a single leading unary operator") and the handoff's claim precisely — honest, disclosed limitation, not a hallucination. |
| `void function(){...}()` | Yes | **directly substantiates** the handoff's claim that the unary generalization covers `!`, `void`, `+`, `-`, `~` — not just `!`; confirmed live, not merely asserted. |

**Grammar-identity claim (write-ahead handoff, `.autodev/handoffs/T-15.md`):** independently
dumped Shape 1 through both `javascript` and `typescript` grammars via `Language.load` +
`rootNode.toString()` — byte-identical trees, matching the handoff's claimed tree
character-for-character. Also dumped Shape 4 (`!`) and the `.call(this)` form independently;
both matched the handoff's prose exactly. **The parse-tree research in `3a943ec` is real,
not fabricated.**

**No gating finding in this file.** AC1's literal, enumerated forms (bare call, arrow call,
`!`-prefixed) are all correctly recognized, both grammars, and the one-level-recursion
invariant holds under adversarial testing, not just the author's own fixtures.

---

## Angle 3 — `plan.ts`: the exported-or-all fallback and `isEligibleCandidate`

**Checked:** read `src/build/plan.ts:223` (`isEligibleCandidate`) and `:243-247`
(`selectLoadBearing`), then built fixtures through the real `digest()`/`buildPlan()` path
(not synthetic `SymbolRecord[]`, since neither function is exported) for: an exported class
with methods, a variable at exactly 2 vs. exactly 3 lines, a TS file exporting only
`interface`/`type`/`enum`, a TS file exporting only a 1-line `const`, a Python file with only
`_private` top-level defs, and a TS file with zero `export` statements at all. Each case run
against current `HEAD`, then against `main`'s `plan.ts` swapped in
(`git checkout main -- src/build/plan.ts`, run, `git checkout HEAD -- src/build/plan.ts`,
confirmed restored and full suite still 185/185 green) to isolate exactly what this ticket
changed.

**Variable span boundary: confirmed exact.** A variable spanning exactly 2 lines never
becomes a candidate; exactly 3 lines does (`isEligibleCandidate`'s `>= 3` is not off-by-one
in either direction) — verified live, not just read.

**Method inside an exported class: unaffected, confirmed.** `export class Widget { make(){}
render(){} }` still yields exactly one symbol step (the class), both before and after this
ticket — the pre-existing overlap-skip still wins because this path never reaches the
fallback (`exported.length > 0`), so T-15 changed nothing here. No finding.

**Snapshot test is real, not decorative.** `test/build-scripts.test.ts`'s "byte-identical
plans" test compares `{kind, chapter, file, id}` for every step via `toEqual` on an array —
structure and ids, not a bare count. Additionally ran that one test
(`npx vitest run test/build-scripts.test.ts -t "byte-identical"`) against `main`'s
`plan.ts`/`extract.ts` swapped in: it **passes there too**, proving the ids genuinely never
moved for an all-exported-functions fixture — this is a real regression guard, not a
tautology dressed as one.

### Gating finding: `isEligibleCandidate` is not scoped to the fallback branch, and measurably changes TS/ESM and Python behavior the spec calls out-of-scope

`src/build/plan.ts:245`:
```ts
const candidates = (exported.length > 0 ? exported : symbols).filter(isEligibleCandidate);
```
The triviality filter runs over **both** branches — the pre-existing "candidates are the
exported symbols" path, not only the new "file exports nothing" fallback. Concretely, with
`main`'s `plan.ts` swapped in against the *same* fixtures:

| Fixture (all real exports, fallback never triggers) | `main` (pre-T-15) | This branch |
|---|---|---|
| TS file exporting only `interface Widget {...}`, `type WidgetId = string`, `enum WidgetKind {...}` | 3 symbol steps (`Widget`, `WidgetId`, `WidgetKind`) | **0** |
| TS file exporting only `export const PI = 3.14159;` (1 line) | 1 symbol step (`PI`) | **0** |

And in the other direction, the fallback fires for languages/files the spec says are
unaffected:

| Fixture | `main` (pre-T-15) | This branch |
|---|---|---|
| Python file with only `_private` top-level defs (all `exported: false`) | 0 symbol steps | **2** (`_helper_one`, `_helper_two`) |
| TS file with zero `export` statements, real top-level functions | 0 symbol steps | **1** (`registerGlobal`) |

`SymbolKind` (`src/types.ts:59`) includes `interface`/`type`/`enum` as first-class kinds
alongside `function`/`method`/`class`/`variable`; `isEligibleCandidate` (`plan.ts:223-228`)
only allows `function`/`method`/`class` unconditionally and `variable` conditionally —
`interface`/`type`/`enum` fall through to `return false` unconditionally, in *every* branch,
not only the fallback. Before this ticket, `selectLoadBearing` had no kind filter at all: any
exported symbol was a candidate.

This is a real, verified regression for an entire class of ordinary TypeScript files (any
module whose public surface is types — a `types.ts`, an `interfaces.ts`, a file exporting one
short constant) that were never mentioned in this ticket's symptom, touched set, or acceptance
criteria's motivating example, and it directly contradicts the spec's own text
(`.autodev/specs/T-15-script-symbols.md`, "Out of scope: … Python and TS/ESM behaviour —
unchanged by design"). None of T-12's 49 existing fixtures, and none of the 9 new
`build-scripts.test.ts` cases, exercise an exported-interface/type/enum-only file, a short
exported constant, a private-only Python file, or a zero-export TS file — so nothing in the
green suite would catch this, and it is not mentioned anywhere in the handoff's otherwise
thorough "Surprises" section.

To be fair to the builder: AC2's own literal text — "candidates are the exported symbols…
**In both cases** a candidate must be a function/method/class, or a variable spanning >= 3
lines" — does say "in both cases," which is exactly what got built. The spec is internally
in tension (AC2's literal words vs. the "Out of scope" line a few paragraphs later), and the
implementation followed the AC. That tension is a spec defect as much as an implementation
one, but the burden was on the builder to surface a change of this size (loses steps on a
whole class of real files; gains steps on two languages/situations the spec explicitly
excludes) rather than let it ride silently under "in both cases." **This needs an explicit
decision — scope `isEligibleCandidate` to the fallback branch only (matching "unchanged by
design"), or get the spec's out-of-scope line corrected and add tests for the now-intended
Python/TS-script behavior — not silence.** Classed gating because it is reproducible, has
real blast radius on ordinary repos, and is asserted nowhere.

No waiver granted — this is not the reviewer's call to make unilaterally; it needs either a
code fix or an operator/PM decision reconciling the spec text before this can pass.

---

## Angle 4 — regression + hygiene

**Checked, all commands run live in the worktree:**
- `npm run typecheck` — clean, no output, exit 0.
- `npm test` — **185/185 passed** across 4 files: `build-scripts.test.ts` (9),
  `pipeline.test.ts` (118), `build.test.ts` (49), `consumer.test.ts` (9). Matches the
  handoff's claimed number and breakdown exactly.
- `git diff main...HEAD -- test/build.test.ts` — empty. T-12's review suite is byte-unchanged.
- `git diff main...HEAD -- test/pipeline.test.ts` — empty. Python/ESM extract expectations
  untouched (and still 118/118 green in the full run above).
- DO-NOT-TOUCH set, each individually diffed against `main...HEAD`: `src/interpret.ts`,
  `src/ask.ts`, `src/cli.ts`, `src/build/interpret.ts`, `src/build/index.ts`,
  `test/build.test.ts`, `package.json`, `src/skins.ts`, `src/assets.ts` — all empty diffs.
- `git diff main...HEAD --name-only` — exactly `.autodev/handoffs/T-15.md`,
  `kb/CURRENT-WORK.md`, `src/build/plan.ts`, `src/extract.ts`,
  `test/build-scripts.test.ts`. No `.autodev` ledger path (`tickets`, `events.jsonl`,
  `releases`) touched.
- `package.json`/`package-lock.json` diff against `main` — empty, no new dependency.
- NUL-byte grep on every changed file — clean.
- `git log --format='%H %an <%ae>'` on all 5 T-15 commits — all `Corgea
  <blackmapartistry@gmail.com>`, correct.
- `kb/CODE-MAP.md` diff against `main` — empty, matching the handoff's "true no-op" claim
  (both new helpers are unexported, and `src/build/**` was already outside the code-map's
  view before this ticket).
- `kb/CURRENT-WORK.md`'s new T-15 bullet lands directly above a pre-existing, already-orphaned
  continuation of the T-11 entry (that entry is missing its own opening bullet line **on
  `main` already**, confirmed via `git show main:kb/CURRENT-WORK.md`) — a pre-existing wart
  in the file, not introduced or worsened by this diff. Noted, not a T-15 finding.

No gating finding.

---

## Angle 5 — the live claim: `plan` on sql-gauntlet

**Checked:** cleared `sql-gauntlet/.repo-tour/` entirely (not reusing the builder's own
cached run) and ran `npx tsx src/cli.ts plan "/home/corgea/Desktop/Coding
Projects/sql-gauntlet"` from this worktree myself.

**Result: `5 chapters · 61 steps · 15 files to reproduce`** — exactly the handoff's claimed
totals (5 shape + 21 file + 35 symbol). Read `.repo-tour/build/plan.json` back and counted
symbol steps per file:

| File | Symbol steps | Names |
|---|---|---|
| `public/app.js` | 5 | viewQuestion, viewGauntletSetup, renderGauntletQuiz, viewGauntletRun, renderQuizBody |
| `public/chat.js` | 5 | init, quickActions, renderMarkdownLite, renderMessages, sendMessage |
| `public/engine.js` | 5 | runStatements, normalizeCell, grade, describeSchema, Engine |
| `public/xray.js` | 5 | tokenize, matchStart, extractItems, parse, attach |
| `server.js` | 5 | buildContextBlock, chatViaSdk, chatViaCli, serveStatic, server |
| `tools/validate.js` | 5 | argSection, fail, note, isNonEmptyString, main |
| `tools/xray-test.js` | 5 | ok, types, clausesOf, findClause, inputs |

Every name and every count matches the handoff's table exactly — no hallucination in this
claim. Confirmed `public/engine.js` really is the UMD-parameter shape
(`(function (root) {...})(typeof self !== "undefined" ? self : this);`, tail of the file) and
`tools/xray-test.js` really does contain eleven `(function` top-level IIFE test blocks
(`grep -c '^(function' tools/xray-test.js` → 11), both matching the handoff's specific claims
about these two files.

**Sanity-checked one range against real source:** `public/app.js`'s `viewQuestion` step
reports `target: {startLine: 258, endLine: 498}`. Line 258 of the real file is
`function viewQuestion(qid, gauntletMode) {`; the range is a real function, not a
misattributed span.

**Independently re-verified the nothing-vanishes invariant and id-uniqueness** on this real
run (not the unit fixtures): 36 inventoried files, 21 taught + 15 reproduced, union = 36,
zero missing, zero double-counted, 61/61 step ids unique. Matches the handoff's claim.

No finding. Left the regenerated `.repo-tour/` cache in place (writing there is explicitly
allowed by this review's brief); nothing else under `sql-gauntlet/` was touched.

---

## Angle 6 — hallucination check

Cross-checked every checkable, specific claim in the handoff against independent
re-derivation rather than re-reading the same assertion:
- Parse-tree shapes for Shape 1 (both grammars), Shape 4, and the double-unary/`.call` cases
  the handoff doesn't cover — all independently dumped, all matched.
- The "byte-identical ids" snapshot — independently proven to also pass against `main`'s
  actual pre-fix code, not merely asserted.
- The 185-test breakdown, the sql-gauntlet 61-step/35-symbol-step numbers, the per-file
  names, the `engine.js` UMD shape, and `xray-test.js`'s eleven IIFE test blocks — all
  independently reproduced and matched exactly.
- `dd267cb`'s RED claim ("7 of 9 fail") — independently reproduced exactly.

**No fabrication found.** The one substantive gap is an omission, not a false statement: the
handoff's "Surprises" section — otherwise unusually forthcoming (it flags the unary
generalization, the grammar-identity result, the `xray-test.js` dual-shape file, the
code-map no-op, even the deleted throwaway scripts) — never mentions that
`isEligibleCandidate` reaches the already-exported path, or that this measurably changes
output for TS/ESM and Python files (Angle 3's gating finding). Given how thorough the rest of
the document is, this reads as a real blind spot (the builder's own prose about the filter
focuses only on the CommonJS/fallback motivating case) rather than concealment, but it is the
kind of side effect this adversarial pass exists to catch.

---

## Verdict

**REWORK.** The ticket's actual named bug (script-style JS producing zero symbol steps) is
fixed correctly and is unusually well-evidenced — every specific, checkable claim in the
handoff reproduced exactly under independent testing, including the live sql-gauntlet numbers
and the grammar research. The blocking issue is scoped and specific: `isEligibleCandidate`
(`src/build/plan.ts:223`, wired at `:245`) applies to the pre-existing exported-symbols path
as well as the new fallback path, which (a) silently removes symbol steps from any ordinary
TS/ESM file whose exports are `interface`/`type`/`enum` or a short constant, and (b) newly
surfaces private Python symbols and zero-export TS scripts — both directions contradicting
the spec's explicit "Python and TS/ESM behaviour — unchanged by design," neither covered by
any existing or new test, and neither disclosed in the handoff. Fix (scope the filter to the
fallback branch, or get explicit sign-off + tests for the wider behavior) and re-submit for
review; everything else in this diff can stand as-is.

---

## Attempt 2

Verdict: PASS

Reviewer seat, auto-review stage, attempt 2. Fresh session, no memory of attempt 1's
session — everything below was independently re-derived from the repo, the three rework
commits, and live runs, not trusted from the handoff's prose. Same worktree
(`/home/corgea/Desktop/Coding Projects/repo-tour-T-15`, branch `feature/T-15-script-symbols`),
same base (`main` = `94e66e5`, still a direct ancestor — `git merge-base main HEAD` ==
`main`, no drift). Scope of this pass: is the one gating finding from attempt 1 actually
closed, does the fix regress anything else, and do the handoff's own self-corrections hold
up. `git status --short` was clean before and after every check except the same
pre-existing, unrelated `.autodev/conformance-history.jsonl` background-monitor edit noted
in attempt 1 and in the handoff — left untouched throughout.

**Summary:** the finding is closed correctly and narrowly — one branch moved
(`src/build/plan.ts:264`), the exported path is now provably byte-for-byte the same
selection main made before this ticket ever existed, and the fallback path's screen was
completed into a closed, exhaustive union instead of merely re-scoped. Verified against
five fixtures I built independently (not copied from `test/build-scripts.test.ts`) through
the real `digest()`/`buildPlan()` path, plus a span-boundary probe. Every regression/hygiene
item in the brief checks out: typecheck clean, 190/190 green, `build.test.ts` and
`pipeline.test.ts` byte-identical to `main`, `src/extract.ts` untouched since attempt 1, the
full DO-NOT-TOUCH set and `package.json`/`package-lock.json` all empty diffs, no NUL bytes,
sql-gauntlet unchanged at 61 steps/35 symbol steps with a fully-cleared cache. The handoff's
three self-reported corrections (the `code-map.mjs` retraction, Surprise 6's disclosure) all
read true against the repo.

---

### 1 — the exported path: candidates are exactly T-12's set

Read `src/build/plan.ts:255-266` (`selectLoadBearing`) and `:218-243`
(`TRIVIAL_SPAN`/`ALWAYS_ELIGIBLE`/`isEligibleCandidate`). The fixed line, `plan.ts:264`:

```ts
const candidates = exported.length > 0 ? exported : symbols.filter(isEligibleCandidate);
```

`isEligibleCandidate` now runs only inside the `: symbols.filter(...)` arm — it is no
longer reachable at all when `exported.length > 0`. Diffed against `main`'s own
pre-T-15 `selectLoadBearing` (`git show main:src/build/plan.ts`, lines 225-233 there):

```ts
function selectLoadBearing(symbols: SymbolRecord[]): SymbolRecord[] {
  const exported = symbols.filter((s) => s.exported);
  const bySpanDesc = exported.slice().sort(...)
```

`main` sorts `exported` directly, no filter of any kind. Current `HEAD`, when
`exported.length > 0`, sorts that exact same unfiltered `exported` array — the two are now
provably identical on the exported path, not merely "looks unfiltered."

**Independently reproduced the attempt-1 reviewer's own two repro fixtures**, built fresh
(different variable names/content, same real `digest()`/`buildPlan()` path, no import from
`test/build-scripts.test.ts`), run live against this `HEAD`:

| Fixture (exported path; fallback never fires) | Result |
| --- | --- |
| TS module exporting only `interface Widget{id}`, `type WidgetId=string`, `enum WidgetKind{Button,Panel}` | `["Widget","WidgetId","WidgetKind"]` — 3 steps |
| TS module exporting only `export const PI = 3.14159;` (1 line) | `["PI"]` — 1 step |

Both match `main`'s behaviour exactly (3 and 1, per attempt 1's table) and match what the
reviewed code (`93bed9f`) got wrong (0 and 0). Probe files (`__reviewer_probe.ts`) written
at the worktree root, run via `npx tsx`, deleted immediately after — `git status` confirmed
clean before the next step.

**`isEligibleCandidate` covers all seven `SymbolKind` members, no silent fall-through.**
`src/types.ts:59-66` — `SymbolKind` is `function | method | class | interface | type | enum
| variable`, seven total. `ALWAYS_ELIGIBLE` (`plan.ts:226`) is a `Set` of exactly five:
`function`, `method`, `class`, `interface`, `enum`. The remaining two, `variable` and
`type`, fall to the span check (`s.endLine - s.line + 1 >= TRIVIAL_SPAN`), never to a bare
`return false`. 5 + 2 = 7, closed and exhaustive by construction — an eighth `SymbolKind`
added later would need to be added to `ALWAYS_ELIGIBLE` deliberately to become
unconditionally eligible, but could never silently vanish the way `interface`/`type`/`enum`
did in attempt 1's version (there, unlisted kinds fell through an explicit `return false`).
No finding.

---

### 2 — the fallback path: re-verified, plus a boundary re-check the rework's refactor made worth redoing

Three more independent fixtures, same method:

| Fixture (file exports nothing; fallback fires) | Result |
| --- | --- |
| Python module, only `_alpha`/`_beta` top-level defs (`_`-prefixed ⇒ `exported:false`) | `["_alpha","_beta"]` — both def steps |
| TS side-effect script, zero `export` statements, one function + a bare call | `["initThing"]` — the function only, not the call |
| No-export JS: one-line `const os = require('os')` + a 20-line function | `["bigWork"]` — the require correctly excluded |

All three match the corrected spec's restated intent and the rework's own tests (c)/(d)/(e)
in substance, using different fixture content.

**Span-boundary re-check.** The rework restructured the predicate (closed `Set` +
named `TRIVIAL_SPAN` constant instead of the old `if`-chain), so the boundary was worth
re-proving rather than trusting the formula reads the same on paper. Built a variable
spanning exactly 2 physical lines and one spanning exactly 3, both zero-export TS files
(fallback path):

- Exactly 2 lines (`const SHORT = {` / `  a: 1 };`): `[]` — excluded.
- Exactly 3 lines (`const LONG = {` / `  a: 1,` / `};`): `["LONG"]` — included.

Matches attempt 1's "confirmed exact, not off-by-one in either direction" finding on the
original code — the refactor did not move the boundary. Also checked a one-line `type`
alias (`type Foo = string;`) alongside a real function in a zero-export file: result
`["useFoo"]` only — the type alias correctly excluded at span 1, confirming `type`'s new
route through the span check (rather than attempt 1's unconditional `false`) doesn't
over-admit trivial aliases. No finding. Probe files (`__reviewer_probe2.ts`,
`__reviewer_probe3.ts`) deleted immediately after use; `git status` clean.

---

### 3 — test quality, and the RED commit reproduced

Re-read all five new `describe` blocks in `806c847`'s diff: each ends in
`expect(names).toEqual([...])` against a full, ordered name array (`['Widget', 'WidgetId',
'WidgetKind']`, `['PI']`, `['_helper_one', '_helper_two']`, `['registerGlobal']`,
`['collectSources']`) — none use a bare `.length`/count assertion, so a passing count with
the wrong symbol chosen would still fail. Confirmed by reading, matching the brief's
requirement.

**Reproduced `806c847`'s claimed RED state live.** `806c847`'s own diff touches only
`test/build-scripts.test.ts` (`src/build/plan.ts` is untouched by that commit — its parent
`93bed9f` already has the reviewed, pre-fix `plan.ts`), so
`git checkout 806c847 -- test/build-scripts.test.ts src/build/plan.ts` pins both to that
commit's exact state. `npx vitest run test/build-scripts.test.ts`:

```
Test Files  1 failed (1)
     Tests  2 failed | 12 passed (14)
```

with the two failures exactly as claimed: (a) `expected [] to deeply equal [ 'Widget',
'WidgetId', 'WidgetKind' ]`, (b) `expected [] to deeply equal [ 'PI' ]`. Matches the commit
message's own numbers, the handoff's discrimination table's middle row, and attempt 1's
Angle 3 table precisely. Restored with `git checkout HEAD -- test/build-scripts.test.ts
src/build/plan.ts`, confirmed `git diff HEAD` empty, reran: 14/14 green again.

---

### 4 — regression + hygiene

All commands run live in the worktree, in this session:

- `npm run typecheck` — clean, no output, exit 0.
- `npm test` — **190/190 passed**, 4 files: `build-scripts.test.ts` (14, up from 9 — the 5
  rework tests), `pipeline.test.ts` (118), `build.test.ts` (49), `consumer.test.ts` (9).
  Matches the handoff's claimed 190 exactly.
- `git diff main...HEAD -- test/build.test.ts` — empty. `git diff main...HEAD --
  test/pipeline.test.ts` — empty. Both still byte-identical to `main`, as both attempt 1
  and the handoff claimed.
- `git diff 93bed9f..HEAD -- src/extract.ts` — empty. Confirmed `src/extract.ts` has had
  zero changes since the attempt-1 review, exactly as the handoff states — this rework
  touched only `src/build/plan.ts` and the test file.
- DO-NOT-TOUCH set, each diffed individually against `main...HEAD`: `src/interpret.ts`,
  `src/ask.ts`, `src/cli.ts`, `src/build/interpret.ts`, `src/build/index.ts`,
  `test/build.test.ts`, `src/skins.ts`, `src/assets.ts`, `package.json`,
  `package-lock.json` — all empty diffs. No new dependency.
- `git diff main...HEAD --name-only` — exactly six paths: `.autodev/handoffs/T-15.md`,
  `.autodev/reviews/T-15.md`, `kb/CURRENT-WORK.md`, `src/build/plan.ts`, `src/extract.ts`,
  `test/build-scripts.test.ts`. No `.autodev/tickets`, `events.jsonl`, or `releases` path
  touched.
- NUL-byte grep (`grep -qP '\x00'`) on all five non-review changed files — clean.
- `git log --format='%H %an <%ae>' 93bed9f..HEAD` — `c081c63`, `3e75bb2`, `806c847` all
  `Corgea <blackmapartistry@gmail.com>`. Correct.

**sql-gauntlet, re-run from a fully cleared cache.** Deleted
`sql-gauntlet/.repo-tour/` entirely (not just `build/`) and ran
`npx tsx src/cli.ts plan "/home/corgea/Desktop/Coding Projects/sql-gauntlet"` fresh from
this worktree: **`5 chapters · 61 steps · 15 files to reproduce`**. Read
`.repo-tour/build/plan.json` back: `{shape: 5, file: 21, symbol: 35}` = 61, and every one of
the 7 named files still hits the 5-symbol cap with the same names attempt 1 and the handoff
both reported (`public/app.js`, `chat.js`, `engine.js`, `xray.js`, `server.js`,
`tools/validate.js`, `tools/xray-test.js`). 61/61 step ids unique. Byte-for-byte the same
totals as before this rework, which is exactly what should happen — sql-gauntlet's JS files
all take the fallback path, and this rework only changed the exported-path branch.

No gating finding in this section.

---

### 5 — the handoff's honesty corrections, checked against the repo

- **`scripts/code-map.mjs` retraction.** `git ls-tree main scripts/` and
  `git ls-tree HEAD scripts/` both list exactly one file, `scripts/fetch-images.mjs` — no
  `code-map.mjs` on either ref. Confirms the handoff's Surprise 4 correction: attempt 1's
  claim to have run `node scripts/code-map.mjs` cannot have been literally true, and the
  rework is right to flag rather than quietly drop it. The underlying no-op conclusion
  still holds on its own: `git diff main...HEAD -- kb/CODE-MAP.md` is empty regardless of
  which regenerator (if any) produced it.
- **Surprise 6 (fallback fires in every language, not just JS) — independently confirmed,
  not just re-asserted.** My own Python and TS fallback fixtures in §2 above (`_alpha`/
  `_beta`, `initThing`) reproduce this directly: a Python module of only `_private` defs
  and a zero-export TS script both mint symbol steps that `main` would have produced zero
  of. This is real and is now stated plainly in both the handoff and the corrected spec,
  not left implicit.
- **The spec correction itself.** This worktree's own `.autodev/specs/T-15-script-symbols.md`
  is unchanged since `main` (`git diff main...HEAD` on that path is empty) — still reads
  "Python and TS/ESM behaviour — unchanged by design," the old wording. That is consistent
  with the handoff's claim that correcting the ledger's spec text is not the coder seat's
  change to make and was deliberately left alone in this worktree. Per this review's brief,
  read (read-only, no edits made) the shop's main checkout
  (`/home/corgea/Desktop/Coding Projects/repo-tour`, on `main`) to find the authoritative
  corrected copy: its `.autodev/specs/T-15-script-symbols.md` out-of-scope line now reads
  "Python and TS/ESM behaviour for files that EXPORT something — unchanged by design (a
  types-only module keeps its interface/type/enum steps; a 1-line exported const keeps its
  step). Files that export nothing take the same exported-or-all fallback as JS — that is
  AC2's language-neutral intent (review finding 2026-09-04)" — verbatim what the handoff
  quotes. One loose end, not gating: that checkout's AC2 line itself still literally reads
  "In both cases a candidate must be a function/method/class, or a variable spanning >= 3
  lines," unrevised — the same internal tension attempt 1 already flagged between AC2's
  literal words and the out-of-scope line. Only the out-of-scope line (the line attempt 1's
  finding actually cited) was corrected; AC2's own wording was not brought into line with
  it. Advisory only: the authoritative out-of-scope line, the implementation, and every new
  test now all agree with each other, so nothing here affects this ticket's own behaviour —
  but the next person reading AC2 in isolation could still be misled, and closing that
  wording gap is a spec-hygiene item for the PM/operator seat, not this ticket.

No gating finding.

---

### Verdict

**PASS.** The one gating finding from attempt 1 — `isEligibleCandidate` reaching the
already-exported candidate path — is closed by a one-branch change
(`src/build/plan.ts:264`) that restores the exported path to a provable byte-for-byte match
with `main`'s pre-T-15 behaviour, while completing the fallback screen into a closed,
exhaustive union over all seven `SymbolKind` members instead of the silent
`return false` attempt 1 shipped. Verified independently, not by re-reading the shipped
tests: five fresh fixtures through the real pipeline reproduce both the attempt-1
reviewer's original two failing cases (now passing) and the three fallback cases (still
passing), a span-boundary probe confirms the `>= 3` line threshold did not move under the
refactor, and `806c847`'s claimed RED (2 failed/12 passed, `(a)` and `(b)` at 0-not-3 and
0-not-1) reproduces exactly against the reviewed code before the fix. No regression:
typecheck clean, 190/190 green, the T-12 and T-15-attempt-1 test files byte-identical to
`main`, the full DO-NOT-TOUCH set and dependency manifests untouched, no NUL bytes, commit
authorship correct, and the live sql-gauntlet run unchanged at 61 steps/35 symbol steps
with the cache fully cleared. The handoff's self-reported corrections (the `code-map.mjs`
retraction, the language-neutral-fallback disclosure) both check out against the repo. One
non-gating advisory: the shop's authoritative spec still has AC2's literal "in both cases"
wording sitting unrevised alongside its now-corrected out-of-scope line — a pre-existing
spec-hygiene loose end, not something this ticket's code or tests need to resolve.
