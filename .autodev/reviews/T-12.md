Verdict: REWORK

# T-12 review — the build-order engine (digest → BuildPlan, and the structural check)

Reviewer seat, auto-review stage. Fresh session, adversarial pass. Worktree:
`/home/corgea/Desktop/Coding Projects/repo-tour-T-12`, branch `feature/T-12-build-engine`,
diffed against the true merge-base with `main` (`b20f8834a453bb7366c6ee91bd73f509b1a4630c`) —
`main`'s tip has since gained T-11 (`package.json` exports map, `src/assets.ts`), so a
plain `main...HEAD` `package.json` diff is misleading; the merge-base diff is the honest one
and is what is reported below.

**Summary:** the engine is well-built and well-tested (154/154 tests green, clean typecheck,
clean hygiene — no DO-NOT-TOUCH touches, no new dependency, no model calls, no NUL bytes, no
hard-coded paths). Angle 1's data-model fidelity check comes back clean: `src/build/types.ts`
implements spec §4.2 verbatim, field for field, with only an additive `reproduce` field the
ticket's own AC2 requires. But adversarial testing (not just reading) found four real,
reproducible defects serious enough to gate: a step-id collision that breaks the "id is
unique" contract, a whole file-classification bucket (`data`) that silently vanishes from
the plan, a Python stub shape that produces code real Python rejects (masked because the
test suite's own parse-check uses tree-sitter's more lenient grammar), and a witness that
goes null for any file introduced by a rename even though real history exists for it.

---

## Angle 1 — data-model fidelity (`src/build/types.ts` vs T-1 spec §4.2)

Checked: line-by-line diff of every interface in `src/build/types.ts` against spec §4.2
(`VSCode-LLM-Tutorial/.autodev/specs/T-1-build-tutorials-v1.md` §4.2), field by field, then
`npm run typecheck` to confirm the `cost: InterpretCost` really typechecks against the real
`../interpret.js` export rather than a shadow copy.

**Result: clean, no gating finding.** Every field in §4.2 is present, correctly typed, and
unrenamed:
- `mode: 'recreate' | 'guided' | 'idea'` — present verbatim (`types.ts:99`).
- `source` union (`repo | idea | both`), each variant's fields exact — present verbatim
  (`types.ts:94-97`).
- `chosen: string | null` — present (`types.ts:78`).
- `whySource: 'interpret' | 'docstring' | 'none'` — present (`types.ts:81`).
- `witness { sha, date, subject }` all `string | null` — present (`types.ts:89`).
- `scaffold { loadBearing, boilerplate }` as `Range[]` — present (`types.ts:85`).
- `dependsOn: string[]` — present (`types.ts:87`).
- `Option { id, label, consequence, taken }` — present verbatim (`types.ts:34-39`).
- `cost: InterpretCost` — imported from `../interpret.js` (`types.ts:18`), not a local
  redefinition; `npm run typecheck` passes clean, so the value `plan.ts` actually builds
  (`plan.ts:481-486`) is structurally assignable to the real type.
- `BuildPlan.reproduce: string[]` is an *addition* beyond §4.2, not a narrowing — and it's
  the ticket's own AC2 ("generated/vendored/lockfile ... listed in plan.reproduce"), layered
  on top of the parent model rather than contradicting it. Not a finding.

`CheckReport` (`types.ts:115-120`) is T-12's own addition (not in §4.2 — the locate doc
confirms it's this ticket's own shape) and matches the locate doc's `.autodev/locates/T-12.md`
description exactly.

## Angle 2 — ordering correctness

Checked: read `dependencyOrder`/`chapterEdges` (`plan.ts:48-119`) and `buildArchitecture`'s
`layerOf` (`architecture.ts:107-142`) in full; built a live 2-file import cycle fixture and
ran it through the real `buildPlan`; ran `buildPlan` twice on the same digest to check
determinism; grepped for `Date.now`/nondeterministic iteration in the ordering path.

- **Leaves-first, verified**: in the shipped test suite's own DAG fixture describe block
  (`test/build.test.ts:60-258`, the exact-equality assertion at `:115-130`)
  `alpha/base.ts` → `alpha/mid.ts` and `beta/top.ts` → `beta/second.ts`, both leaves-first,
  exact-equality-checked. Confirmed again independently with my own fixture (below).
- **Cycle handling — tested live.** Built `src/a.ts` ⇄ `src/b.ts` (mutual import) plus
  `src/c.ts`. Ran `buildPlan`: both `a.ts` and `b.ts` appear as file steps (no drop), no
  duplicate file steps, no dangling `dependsOn` ids, and two separate `buildPlan` calls on
  the same digest produced byte-identical file order (`["src/c.ts","src/a.ts","src/b.ts"]`
  both times). The cycle-break rule (`plan.ts:84-92`, forced placement of the node with
  fewest unmet deps, path as final tiebreak) terminates and never drops a node: `remaining`
  strictly shrinks every iteration since a forced single-node wave is still a non-empty wave.
- **Bottom-up chapters, verified functionally, but independently re-derived — advisory.**
  `plan.ts` does not consume `Architecture.layers` (already computed by `architecture.ts`,
  entry-points-first/foundations-last) or `Architecture.edges` (already deduped and counted).
  It re-derives a chapter order from scratch via `chapterEdges(digest.graph.edges, ownerOf)`
  (`plan.ts:102-119`) + the same `dependencyOrder` used for files. This does satisfy AC1's
  literal text ("ordered after the chapters they import from") and my cycle testing shows it
  terminates deterministically — but it is a second, independent implementation of what
  `buildArchitecture` already computed, with a *different* cycle-break heuristic (fewest
  unmet deps + path, vs. `architecture.ts`'s fewest current importers) — see Angle 2
  advisory below. Not gating: no incorrect output was produced by either algorithm on any
  fixture tried, including a forced cross-chapter-style cycle.
- **Determinism**: the only `Date`/`Date.now()`-shaped call anywhere in `src/build/**` is
  `generatedAt: new Date().toISOString()` (`plan.ts:494`), a metadata timestamp never read
  for ordering. No ordering step reads Map/Set iteration order for anything but membership
  tests; every sort has an explicit, total tiebreak. Confirmed empirically (see above): same
  digest in ⇒ same steps out, twice.

**Advisory 2a** — `plan.ts:102-119, 309-357` duplicates `architecture.ts:107-142`'s
part-level edge rollup and layering rather than reusing it. Functionally correct today; a
future change to either module's cycle-break rule could make the two silently disagree on a
genuine cross-chapter cycle. Recommend a follow-up to have `plan.ts` consume
`Architecture.layers`/`edges` directly, but this does not block T-12.

## Angle 3 — coverage

Checked: `plan.ts:316-346`'s file-bucketing loop; ran the shipped `data`/reproduce
assertions plus a fixture of my own with a binary-ish asset and a JSON data file; checked
`subjectBasename` (`plan.ts:179-187`) against `inventory.ts`'s real `looksLikeTest`
(`inventory.ts:84-95`) for drift.

- `source`/`structural` files: exactly one `file` step each, confirmed by the shipped tests
  and by reasoning over the single-pass, single-bucket assignment in `plan.ts:326-337` (every
  file is visited once, `ownerOf` is a deterministic single-valued function, so no path can
  land in two buckets or the same bucket twice).
- `@misc` catch-all: exercised by the shipped fallback-fixture tests; files with no chapter
  owner are correctly bucketed and get steps, and the synthetic chapter is correctly
  suppressed when nothing lands in it (`hasMisc` gate, `plan.ts:356-357`).
- `generated`/`vendored`/`lockfile`: confirmed only ever `reproduce`d, never stepped
  (`plan.ts:327-330`), matching the shipped test's exact-equality check.
  `test` files: confirmed spliced immediately after the file they name, `conftest.py` and
  path-only (`test/`-segment) test files correctly fall through to "end of chapter" (their
  own documented, intended fallback) — cross-checked `subjectBasename` against
  `inventory.ts`'s `looksLikeTest` patterns and found them consistent (plan.ts's stripping
  regexes are a strict superset in extension coverage but only ever run over
  already-test-classified paths, so this causes no false splices).
- **GATING — `data`-classified files are silently dropped entirely.** `plan.ts:331`:
  `if (f.classification !== 'source' && f.classification !== 'structural' && f.classification
  !== 'test') continue;` — a file classified `data` (real, common: any binary asset via
  `inventory.ts:371`, or any file in an unrecognized language/no recognized structural name
  via `inventory.ts:192`, e.g. images, fonts, JSON/YAML/CSV data) is neither turned into a
  step nor added to `plan.reproduce`. It vanishes from the `BuildPlan` completely. Verified
  live: a fixture with `assets/logo.png` and `config/data.json` alongside two `.ts` source
  files produced a plan where neither data file appears in any step's `target.file` nor in
  `plan.reproduce`. This breaks the parent T-1 spec's own §9 acceptance criterion #3
  ("Walking every step in automated mode reproduces the reference's source files
  byte-for-byte") for any repo containing binary assets or non-code data files — which is
  most real repos, and is very plausibly true of `sql-gauntlet`, T-1's own named first live
  target. T-12's own spec table (§ "ACs", AC2) and the parent spec's §4.1 table are both
  silent on `data` specifically, which is a spec-level gap the implementation inherited
  rather than resolved — noted so the record is fair to the builder, but the resulting
  behavior (silent, total disappearance of real files) is a real product-facing bug, not a
  cosmetic one, so it is graded gating rather than advisory.

## Angle 4 — symbol steps + scaffold

Checked: `selectLoadBearing` (`plan.ts:217-238`) and `complement` (`plan.ts:203-215`) by
hand-traced arithmetic and live fixtures; re-parsed stubbed output through the real
`extract()` for a TS class, an arrow-function export, and a Python method/class, per the
brief.

- **5-cap by span, class/method overlap** — confirmed live: an exported class with 3 methods
  (`export class Widget { a(){} b(){} c(){} }`) yields **exactly one** symbol step, for the
  class itself ("Fill in Widget..."), with the methods correctly subsumed rather than
  double-stubbed — matches the handoff's documented "Surprise #3" resolution exactly.
- **`boilerplate` = exact complement of `loadBearing` in `[1, loc]`** — hand-traced
  `complement()` for interior ranges, whole-file ranges, empty ranges, and end-of-file tails;
  no gap, no overlap, no off-by-one in any case. Cross-checked against the shipped AC2 cap
  test, which asserts `covered + gap === fileLoc` using the *real* `inventory.ts`-recorded
  `loc` (not a hand recount) — a real invariant check, not a tautology.
  boilerplate on this ticket's own file is `[]` correctly (nothing to hide when there is
  nothing exported); the entire file is boilerplate when there are zero exported symbols.
- **Stubs parse — TS class**: confirmed (existing tests + my own fixtures).
- **Stubs parse — arrow-function export**: the shipped test only covers a *single-line*
  arrow (`export const add = (a, b) => a + b;`), which stubs and parses correctly. I tested
  the harder case, a **multi-line, brace-less arrow signature**:
  ```ts
  export const add = (
    a: number,
    b: number,
  ) => a + b;
  ```
  `stubJsLikeRange`'s case 2 (`stub.ts:54-59`) only inspects the range's *first* line for
  `=>`; since `=>` is on line 4 here, case 2 misses it, case 1 (no `{` anywhere) also misses
  it, and the "leave it alone" fallback (case 3) fires: **the body is left completely
  visible, un-hidden**, though the file still parses (0 tree-sitter errors, confirmed).
  Advisory, not gating — see below.
- **Stubs parse — Python method**: the shipped tests cover a normal, multi-line
  `def`/`class` body correctly (own body indentation measured and matched). I tested the
  narrower but very common **one-line** shape (`class EmptyError(Exception): pass`, or
  `def add(a, b): return a + b`) end-to-end through the *real* pipeline (`digest` →
  `extract` → `buildPlan` → `stubFile`), because `extract()` genuinely reports
  `line === endLine` for these (verified with real `extract()` output). See Angle 10/gating
  below — **this is a real bug**, not merely a hypothetical.

**Advisory 4a** — multi-line brace-less arrow signatures are silently left un-hidden
(`stub.ts:54-59`). Never breaks parsing (confirmed), so AC5's literal check ("parses with
zero ERROR nodes") is not violated, and the file's own documented philosophy explicitly
prefers "leave it alone" over risking corrupt output. I am **not** grading this gating,
but it is a real, silent under-hide of load-bearing content for a common TypeScript style
(multi-line typed parameter lists), worth a follow-up.

## Angle 5 — `check()`

Checked: read `check.ts` in full; ran the shipped AC6 tests; independently tested an
unsupported language and temp-dir cleanup live.

- Never compares bodies — confirmed by code (`compare()` only ever reads `symbols`/`kind`/
  `imports[].raw`/`parseErrors`) and by the shipped "completely rewritten implementation ...
  still passes" test.
- Renamed local passes, missing export named, extra export named without failing the check
  on its own, missing import fails — all confirmed via the shipped tests, re-run here green.
- **Learner file that fails to parse**: `report.ok` correctly goes `false` and
  `parseErrors > 0` (confirmed, shipped test + a hand check). There is no separate free-text
  "reason" field in `CheckReport` — only the numeric `parseErrors` count. This is **not** a
  spec violation (`CheckReport` is T-12's own addition, and its shape matches the locate doc
  verbatim), so I am not grading it, but flagging it since the angle asked directly: the
  numeric count is the only signal a caller gets for "why did this fail to parse."
- **Language `extract()` does not support**: tested live with `language: 'ruby'` — `check()`
  does **not** throw; it returns a well-formed report (`parseErrors: 1`, the reference's
  symbol reported `missing`, `ok: false`) — a clean, honest degradation, not a crash.
- **Temp dir cleanup**: verified live — `os.tmpdir()` entries matching `repo-tour-check-*`
  before and after a `check()` call are equal (0 before, 0 after); the `finally` block
  (`check.ts:104-106`) does its job.

## Angle 6 — ids

Checked: read `stepId` (`plan.ts:294-297`) and its two call sites (`plan.ts:403`, `:432`,
`:452`); ran the shipped rename/body-edit test; built and ran a live same-name-collision
fixture.

- Stable across a body-only edit, changes on rename: confirmed by the shipped test
  (`test/build.test.ts:186`, "AC3 — ids survive a body-only edit and change after a
  rename"), re-run here green.
- **GATING — ids are not unique across a plan when two distinct symbols in one file share a
  name.** `stepId(file, kind, symbolName)` (`plan.ts:295-297`) hashes the **step kind**
  (always the literal string `'symbol'` for every symbol step — not the symbol's own
  `SymbolKind`, and not its line number). Built a live fixture: one Python file with two
  legally-redefined top-level functions both named `process` (a real, legal Python pattern —
  a later definition simply shadows an earlier one; also realistic under an
  `if TYPE_CHECKING`/conditional-definition style). Both survive `selectLoadBearing`'s
  overlap filter (their ranges don't overlap each other) and both become symbol steps.
  Result: **both steps got the identical id `78e00db5e49189cb`.** Plan-wide id-uniqueness
  check on that same fixture: 7 steps, only 6 unique ids. This breaks the "id" concept as
  used everywhere else in the model (a `dependsOn` foreign key, a natural React/TreeView
  key, `fileStepId`/`shapeStepId` maps keyed by id elsewhere in this same file) and is
  exactly the scenario Angle 6 asked to be tested for. (I also tried TS function overloads
  as a second collision vector; `extract()` turns out to only record the implementation
  signature, not the overload declarations, so that specific path is clean — recorded so the
  record shows the negative result too.)
- Two files with the same basename: confirmed **not** a collision risk — `file` in the hash
  is always the full relative path, never the basename, verified by code reading (no live
  test needed; the id formula makes this structurally impossible).

## Angle 7 — witness

Checked: read `witness.ts` in full; ran the shipped tests; built two fixtures of my own —
one commit that adds two files at once, and a separate commit that renames a file — and ran
both the raw `git log --diff-filter=A` command `witness.ts` runs and `firstCommits()` itself
against them; also confirmed the actual git classification of the rename with
`git log --name-status` and `--diff-filter=R`.

- **Header-shape parsing (the documented "blank line after header" trap) — verified
  correct.** `HEADER` (`witness.ts:37`) matches by tab-delimited shape rather than blank-line
  position, so it survives the exact trap the handoff describes. Live test: one commit
  adding 3 files at once (`m1.ts`, `m2.ts`, `m3.ts`) all correctly resolve to that commit's
  sha/date/subject; a later commit adding a 4th file resolves separately and correctly. No
  filenames dropped.
- **GATING (spec-gap, not implementation-deviation) — a file introduced solely via a rename
  gets no witness at all, even though it has real history.** Built: commit 1 adds `fileA.ts`
  and `fileB.ts`; commit 2 renames `fileA.ts` → `fileC.ts` (plain `mv` + `git add -A` +
  commit — no `-M` flag anywhere, matching how a learner or `test/build.test.ts`'s own
  rename test does it). Confirmed with `git log --name-status` that **this environment's git
  auto-detects the rename** (`R100 fileA.ts fileC.ts`) even without an explicit `-M` flag —
  so under `--diff-filter=A` (exactly what `witness.ts:55` runs) the rename commit
  contributes **zero** `A` entries; `fileC.ts` never appears under any header. Running the
  real `firstCommits()` against this fixture: `fileB.ts` correctly keeps commit 1's witness;
  `fileC.ts` has **no entry in the map at all**, so `buildPlan` reports
  `witness: {sha:null,date:null,subject:null}` for it — indistinguishable from "no history
  anywhere." AC4's text prescribes exactly this git invocation
  (`git log --diff-filter=A`), so the implementation is not deviating from what was asked —
  but the resulting behavior contradicts the stated intent ("never invented," "shown, never
  used to order," T-1 spec: "a repo with no git history ... gets witness: null ... never
  invents") for a file that in truth has full git history, just not an `A`-classified event
  under its current name. This is graded gating because the observable failure mode is a
  common, realistic repo shape (any file ever renamed) producing a materially misleading
  signal, not a hypothetical corner case — but the record should make clear this is as much
  a gap in AC4's chosen git invocation as it is a defect in `witness.ts`'s implementation of
  it, and a ticket-owner waiver ("acceptable, AC4 says diff-filter=A and that's what
  shipped") would be a reasonable, defensible call.
- Null without `.git` and with an empty `.git` (zero commits): both confirmed via the shipped
  tests, re-run here green (`test/build.test.ts:269-321` fallback fixture's no-git case,
  `:833-852` zero-commit case).

## Angle 8 — DO-NOT-TOUCH + hygiene

Checked against the **true merge-base** (`b20f8834a...`), not `main`'s current tip (which has
moved forward with T-11 and would produce a misleading `package.json` diff).

- `package.json`: byte-identical to merge-base (`diff` empty); zero commits on this branch
  touch it (`git log --oneline main..HEAD -- package.json` empty). No new dependency.
- Forbidden files (`src/extract.ts`, `src/skins.ts`, `src/interpret.ts`, `src/ask.ts`,
  `src/server.ts`, `src/repoview.ts`): `git diff --stat` against the merge-base for exactly
  these paths is empty. Untouched.
- `.autodev/**` ledger: only `.autodev/handoffs/T-12.md` (144 insertions, new file) differs
  from the merge-base under `.autodev/`; `.autodev/tickets/`, `.autodev/events.jsonl`,
  `.autodev/releases/` are untouched — confirmed by `git diff --stat` scoped to exactly
  those paths (empty). Writing the ticket's own handoff is required by the plan (step 8),
  not a ledger touch.
- Model calls: `grep -rn "runLlm\|interpret(" src/build/ src/cli.ts` — no matches.
- NUL/odd bytes: `grep -rlP '\x00' src test schema` — no matches; double-checked with `od`
  on `types.ts` and `plan.ts` (the two files the handoff's own "Surprises" section flags as
  having had a NUL byte at one point) — zero NUL bytes in either; `file(1)` reports plain
  UTF-8 text for every file under `src/build/`. The handoff's claim of having found and
  fixed this is consistent with what ships.
- Hard-coded absolute paths: `grep -rnE "/home/|/Users/|C:\\\\"` over `src/build/*.ts`,
  `src/cli.ts`, `test/build.test.ts`, `schema/*.json` — no matches.
- `kb/CODE-MAP.md` differing from `main` is expected (T-11 has since landed on `main`) and
  is not a finding per the task brief. Separately verified the handoff's own claim that
  `src/build/**` is invisible to the regenerated map (`code-map.mjs`'s `SKIP_DIRS` treats any
  dir literally named `build` as build output): `grep -n "src/build" kb/CODE-MAP.md` — no
  matches. Claim is accurate, correctly self-reported as an out-of-touched-set limitation.

**No gating findings on this angle.**

## Angle 9 — run it yourself

Commands run, with actual results:

- `npm run typecheck` → clean, no errors.
- `npm test` → **154 passed (154)**, 2 files (`test/pipeline.test.ts` 118, `test/build.test.ts`
  36), 0 failed. ~3.6s wall.
- Built a fresh fixture (not reusing any shipped test fixture) at
  `/tmp/.../rt-review-fixture`: a 2-directory-deep TS project (`src/util/format.ts` ← imported
  by `src/core/engine.ts`, plus `src/core/engine.test.ts`, plus a `dist/bundle.js` generated
  file), real git history. Ran the actual CLI: `npx tsx src/cli.ts plan <fixture>`.
  Output: `1 chapters · 6 steps · 1 files to reproduce`, and
  `.repo-tour/build/plan.json` written. Inspected the JSON directly:
  - Chapters/steps ordered exactly as expected: `src` shape → `format.ts` file → `shout`
    symbol → `engine.ts` file → `Engine` symbol → `engine.test.ts` file (spliced right after
    its subject).
  - Every step's `decision.question` reads like an actual question ("Fill in shout — how
    should this function do its job?", "This module exports Engine, run and imports
    ../util/format.js — why does this get its own module?").
  - Every step's `decision.options[0].taken === true` and `.id === decision.authorChoice`
    and `decision.chosen === decision.authorChoice` — confirmed for all 6 steps.
  - `dependsOn` wiring cross-checked by hand against actual ids (not just "looks plausible"):
    `engine.ts`'s file step correctly depends on `format.ts`'s file step id
    (`0bc7fcf4f8528caf`); `engine.ts`'s own `Engine` symbol step and `engine.test.ts`'s file
    step both correctly depend on `engine.ts`'s **own** file step id (`8afac2ef4ef498b2`) —
    no self-loops, no swapped edges.
  - `reproduce: ["dist/bundle.js"]`, `cost` honestly zeroed with `provider:'none'`,
    `metered:false`.
  - Minor cosmetic nit (not gating): the CLI's summary line always says plural
    ("1 chapters"/"1 steps"/"1 files") regardless of count.
  - Schema validity for this plan was not separately re-checked by hand (redundant with
    AC8's own passing "validates a plan generated from a real fixture" test, which does the
    same thing against a similarly-shaped fixture and passed in the `npm test` run above).

## Angle 10 — hallucination check

Checked every quantitative and factual claim in `.autodev/handoffs/T-12.md` against the
actual repo state.

- **Confirmed inaccurate (advisory, not gating): `.autodev/handoffs/T-12.md:60`** claims
  "`test/build.test.ts` — 38 tests." Actual count, both by direct grep
  (`grep -c '^\s*\(it\|test\)(' test/build.test.ts` → 36) and by vitest's own run output
  (`test/build.test.ts (36 tests)`), is **36**, not 38. The handoff's *total* claim ("154
  tests, 2 files", line 5) is correct (118 + 36 = 154) — only the per-file breakdown is
  wrong, by 2.
- Every other checked claim held up under direct verification, not just re-reading:
  - "Nothing on the DO-NOT-TOUCH list was touched" — confirmed (Angle 8).
  - "`package-lock.json` and `.autodev/conformance-history.jsonl` ... neither was staged or
    committed" — confirmed via `git status`/`git log -- package-lock.json`.
  - "7 stub shapes tested" (AC5 table row) — confirmed, 7 `it()` blocks under the AC5
    describe block.
  - The `code-map.mjs` `build`-directory blind spot claim — confirmed true (Angle 8).
  - The NUL-byte-in-`plan.ts` surprise, said to be found and fixed — confirmed fixed, zero
    NUL bytes anywhere in `src/build/**` today.
  - Table row "AC4 | witness from history, null without | met — no-`.git`, zero-commit
    `.git`, and real-history cases all covered" — this is an accurate description of what
    was *tested*, not a false claim; it does not claim to have tested the rename case, which
    is where Angle 7's gating finding actually lives. Not a hallucination, just a coverage
    gap the handoff doesn't overclaim past.
- No other overclaimed behavior found (no claimed-but-missing test, no claimed check that
  doesn't actually run, no silently-skipped AC).

---

## Gating findings (must be resolved or explicitly waived before merge)

1. **Step-id collisions are possible and demonstrated.** `src/build/plan.ts:295-297`
   (`stepId`) plus its symbol-step call site `src/build/plan.ts:452`. Two distinct,
   non-overlapping symbols sharing a name in one file (shown live with a legal Python
   top-level redefinition) get the identical id. Breaks the model's implicit
   uniqueness contract (Angle 6).
2. **`data`-classified files vanish from the plan entirely** — neither taught nor
   reproduced. `src/build/plan.ts:331` (the classification filter). Breaks the parent
   spec's byte-for-byte automated-reproduction promise (T-1 §9-3) for any repo with binary
   assets or non-code data files (Angle 3).
3. **One-line Python `def`/`class` stubs produce invalid Python**, confirmed with a real
   `python3 -m py_compile` failure (`IndentationError: unexpected indent`) on output the
   real pipeline actually generates for a very common pattern (`class X(Exception): pass`).
   `src/build/stub.ts:70-77` (`stubPythonRange`). Not caught by the test suite because its
   own parse-check (tree-sitter's ERROR-node count) is more lenient than real Python here
   (Angle 4/5).
4. **Witness silently reports `null` for any file introduced by a rename**, even though it
   has real git history — demonstrated live; this environment's git auto-detects the rename
   (`R100`) with no flags needed, so `--diff-filter=A` (exactly what AC4 specifies) never
   sees the new path. Noted as gating because the observable failure (a file with real
   history looking exactly like a file with none) is common and materially misleading, but
   flagged as a plausible **candidate for a ticket-owner waiver** since the implementation
   does exactly what AC4's text prescribes (`src/build/witness.ts:46-83`) (Angle 7).

## Advisory findings (do not block, worth a follow-up)

- Handoff overclaims `test/build.test.ts`'s own test count (38 vs. actual 36); total count
  across both files (154) is correct. `.autodev/handoffs/T-12.md:60` (Angle 10).
- Multi-line, brace-less arrow-function signatures are left completely un-hidden by the
  stub writer (never breaks parsing, but silently fails to scaffold). `src/build/stub.ts:54-59`
  (Angle 4).
- Chapter ordering re-derives its own bottom-up sort from raw file edges rather than reusing
  `Architecture.layers`/`edges`, which `buildArchitecture` already computed; functionally
  correct today but a duplicated, independently-tuned cycle-break heuristic.
  `src/build/plan.ts:102-119,309-357` vs. `src/architecture.ts:107-142` (Angle 2).
- CLI always prints plural units regardless of count ("1 chapters"). Cosmetic. (Angle 9)
- `CheckReport` carries no free-text parse-failure reason, only a numeric count — not a
  spec violation, just worth naming since Angle 5 asked. (Angle 5)

## Commands run (for reproducibility)

```
git merge-base main HEAD                                  # b20f8834a453bb7366c6ee91bd73f509b1a4630c
git diff --stat b20f8834..HEAD                             # 12 files, 2171(+)/4(-)
diff <(git show <merge-base>:package.json) <(git show HEAD:package.json)   # empty
git log --oneline main..HEAD -- package.json src/extract.ts src/skins.ts \
  src/interpret.ts src/ask.ts src/server.ts src/repoview.ts                 # empty
grep -rlP '\x00' src test schema                                            # no matches
grep -rn "runLlm\|interpret(" src/build/ src/cli.ts                         # no matches
grep -rnE "/home/|/Users/|C:\\\\" src/build/*.ts src/cli.ts test/build.test.ts schema/*.json  # no matches
npm run typecheck                                                           # clean
npm test                                                                    # 154 passed (154), 2 files
npx tsx src/cli.ts plan <fresh fixture>                                     # "1 chapters · 6 steps · 1 files to reproduce"
```

Plus five purpose-built, throwaway fixtures (each built and torn down with real `git`
repos, exactly the shipped suite's own style) exercising: a 2-file import cycle; a
same-name/two-symbol id collision; a multi-file-single-commit + rename witness scenario;
one-line Python def/class stubs verified against real `python3 -m py_compile`; a
class-with-methods overlap check; an unsupported-language `check()` call; and a `data`
classification coverage probe (binary asset + JSON data file). All experiment scripts
(`review-adversarial{,2,3}.ts`) were deleted after use; `git status` was clean of them
before this record was committed.

---

**Verdict: REWORK.** The engine is fundamentally sound — correct topological ordering
(including on a real cycle), a faithful and complete §4.2 data model, clean hygiene, and a
genuinely strong test suite (154/154 green) — but four real, independently-reproduced
defects (id collisions, dropped `data` files, invalid-Python one-line stubs, null witness on
rename) should be fixed or explicitly waived by the ticket owner before this ships as the
product's intellectual core. None of them are DO-NOT-TOUCH violations, dependency additions,
or model calls, so BLOCK is not warranted.
