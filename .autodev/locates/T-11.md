# Locate — T-11

## Touched set
- `package.json` — add `exports` (with `types` conditions), `files`, `prepare`. **T-11 owns this file outright**; T-12 must not touch it (T-11 pre-adds `"./build": "./dist/build/index.js"` for T-12's module).
- `src/assets.ts` (new) — `configureAssets({ assetsDir?, grammarsDir? })`, `assetsDir()`, `grammarsDir()`: explicit config → `REPO_TOUR_ASSETS` / `REPO_TOUR_GRAMMARS` env → the `import.meta.url` / `require.resolve` defaults.
- `src/skins.ts` — `skinsDir()` reads `assetsDir()/skins` (today: `path.join(dirname(fileURLToPath(import.meta.url)), '..', 'assets', 'skins')`).
- `src/extract.ts` — `grammarPath(name)` reads `grammarsDir()` (today: `require_.resolve('tree-sitter-wasms/package.json')` → `out/`). Touch ONLY `grammarPath`; the parsers map and `extract()` are untouched (T-12's check calls `extract()`).
- `test/consumer.test.ts` (new); `README.md` (one line under "Also").

## Entry points
`src/skins.ts#baseCss/alternateCss` · `src/extract.ts#initParsers` · `package.json#exports`.

## DO-NOT-TOUCH
`src/build/**` (T-12 creates it) · `src/cli.ts`, `src/server.ts`, `src/repoview.ts`, `src/interpret.ts`, `src/ask.ts` (T-13 will touch interpret/ask) · `.autodev/**` ledger.

## Conflicts
T-12 runs in parallel in its own worktree (`../repo-tour-T-12`, branch `feature/T-12-build-engine`). Disjoint files by construction: T-12 = `src/build/**`, `schema/`, `test/build.test.ts`, `src/cli.ts`. The only shared surface is the `./build` export path, which T-11 writes and T-12 fills.
