# Locate — T-12

## Touched set (new unless noted)
- `src/build/types.ts` — `BuildPlan`, `Chapter`, `Step`, `Option`, `Range`, `CheckReport` (VSCode-LLM-Tutorial spec §4.2).
- `src/build/plan.ts` — `buildPlan(digest: DigestResult, opts: { root; witness?: boolean }): Promise<BuildPlan>`.
- `src/build/witness.ts` — first-commit per file from git, one `git log --diff-filter=A --name-only` walk per repo root.
- `src/build/stub.ts` — `stubFile(source, loadBearing: Range[], language, questions): string`.
- `src/build/check.ts` — `check(learnerSource, referenceExtract, language, relPath): Promise<CheckReport>`.
- `src/build/index.ts` — re-exports.
- `schema/build-plan.schema.json` — JSON schema for `BuildPlan`.
- `src/cli.ts` (edit) — add the `plan <path> [--json]` verb beside `digest`/`tour`; reuse its arg parser.
- `test/build.test.ts` (fixtures created on the fly, like `test/pipeline.test.ts`).

## Reads (verified 2026-09-04)
- `src/digest.ts` — `DigestResult { manifest, inventory, extracts, graph, ranked, deepSlice, tiers }`.
- `src/architecture.ts#buildArchitecture` — parts + edges + layers; the chapter source.
- `src/rollup.ts#TierDigest` — fallback chapters (`kind === 'subsystem'`).
- `src/types.ts` — `FileRecord.classification`, `FileExtract.symbols[{name, kind, line, endLine, exported, doc}]`, `ImportGraph.edges`.
- `src/rank.ts` — `RankedFile.score` for tie-breaks. `src/extract.ts#extract` — for the check (call it, never edit it).
- `src/tour.ts` header — the projection doctrine this module follows.

## DO-NOT-TOUCH
`package.json` (T-11 owns it; the `./build` export is pre-added there) · `src/extract.ts`, `src/skins.ts` (T-11 edits them in parallel) · `src/interpret.ts`, `src/ask.ts` (T-13) · `src/server.ts`, `src/repoview.ts` · `.autodev/**` ledger.

## Conflicts
T-11 runs in parallel in `../repo-tour-T-11`. Disjoint by construction; the only touch-point is that `dist/build/index.js` must exist for T-11's `./build` export to resolve — it will once this ticket builds.
