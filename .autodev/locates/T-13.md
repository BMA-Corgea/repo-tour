# Locate — T-13

## Touched set
- `src/interpret.ts` — `StopMeaning.alternatives`; `PROMPT_VERSION = 6`; the `SYSTEM` prompt (+ the JSON shape line); `parseAnswer` accepts `alternatives`; `InterpretOptions.runner?` (injectable, defaults to `runLlm`); the one `runLlm(...)` call site uses it. Nothing else in the file.
- `src/build/interpret.ts` (new) — `interpretPlan(plan, digest, opts): Promise<{ plan: BuildPlan; cost: InterpretCost }>`: builds `CodeStep[]` from `symbol` steps (their ranges) and `file` steps (`1 … min(loc, 120)`), calls `interpretStops`, folds meanings into a NEW plan (`options = [author, ...alternatives]`, `why`, `whySource: 'interpret'`, `cost`).
- `src/build/index.ts` — re-export `interpretPlan`.
- `src/build/types.ts` — no shape change needed (`Option`, `whySource: 'interpret'` already exist); only if a doc comment must be updated.
- `src/ask.ts` — `AskContext.build?`; `buildContextBlock` renders it.
- `src/cli.ts` — the `plan` verb (T-12's) gains `--interpret`, `--cached-only`, reuses the existing `--provider` / `--model` args.
- `test/build.test.ts` — new cases with a stubbed runner; `test/pipeline.test.ts` — the ask context test gains the build block (or a new small `test/ask-build.test.ts`).

## Reads (verified 2026-09-04)
`src/interpret.ts` lines 26–60 (`StopMeaning`), 123–160 (`FileJob`, the `SYSTEM` prompt), 190–225 (`parseAnswer`), 225–340 (`interpretStops`), 373–390 (`applyMeanings` — the fold pattern to mirror) · `src/build/plan.ts` `authorOnlyDecision` (l. 288) — the shape you extend · `src/build/types.ts` · `src/ask.ts` (`AskContext`, `buildContextBlock`, `clip`) · `src/codetour.ts` `CodeStep` · `src/llm.ts` `runLlm`, `resolveChoice`.

## DO-NOT-TOUCH
`src/build/plan.ts`, `src/build/check.ts`, `src/build/stub.ts`, `src/build/witness.ts` (T-12 — merged; do not re-open them) · `src/extract.ts`, `src/skins.ts`, `src/assets.ts` (T-11) · `package.json` · `src/server.ts`, `src/repoview.ts`, `src/prview.ts` · `.autodev/**` ledger.

## Conflicts
None in flight in this shop after T-12 merges. The consumer, VSCode-LLM-Tutorial T-3, waits on this ticket.
