# Locate - T-15

## Touched set
- src/extract.ts - the symbol walk (the function that visits top-level nodes and records SymbolRecords for JS/TS). Add: when a top-level statement is an expression statement whose expression is a call (possibly parenthesized / unary-prefixed) to a function expression or arrow function, walk that function's body statements as if top-level, recording their declarations with exported:false. Do NOT touch grammarPath (T-11) or initParsers.
- src/build/plan.ts - the symbol-step candidate selection (the code that filters symbols by `exported`, sorts by span, applies overlap-skip and the cap of 5). Add the exported-or-all fallback and the triviality filter (function/method/class always eligible; variable only if endLine - line + 1 >= 3).
- test/build-scripts.test.ts (new) - IIFE fixture, CJS server fixture, a plain-nested-helper negative, and the sql-gauntlet-shaped count assertion on a synthetic fixture.
- test/pipeline.test.ts - ONLY if an existing extract expectation must change (it should not: ESM/TS/Python fixtures have no IIFEs).

## Reads
src/extract.ts (whole symbol walk; GRAMMAR_FOR; how `exported` is decided for JS vs Python) · src/build/plan.ts (candidate selection, stepIdMinter, overlap-skip) · src/types.ts (SymbolRecord) · test/build.test.ts (fixture helpers to reuse, not edit) · .autodev/reviews/T-12.md (what reviewers check).

## DO-NOT-TOUCH
src/interpret.ts, src/ask.ts, src/cli.ts, src/build/interpret.ts, src/build/index.ts (T-13, in flight) · src/skins.ts, src/assets.ts, package.json · test/build.test.ts (add a new file instead) · .autodev/** ledger.

## Conflicts
T-13 in parallel (../repo-tour-T-13). Disjoint by construction; both branch from main a8828e5. kb/CURRENT-WORK.md and kb/CODE-MAP.md will conflict trivially at merge (the orchestrator unions them).
