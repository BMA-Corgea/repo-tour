# This repository is public

The owner asked on 2026-09-05 that their name not appear in public repositories.

- Write "the owner" wherever you would write the person's name: code comments, docs, KB pages,
  commit messages, handoffs.
- The factory ledger (`.autodev/`) and the generated day pages (`kb/days/`) are untracked here on
  purpose. Never `git add -f` them.
- Owner-facing working notes go under `kb/notes/` with neutral filenames, never the repo root.
